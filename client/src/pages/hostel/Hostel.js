import React from "react";

const Hostel = ({
  name,
  price,
  city,
  region,
  roomType,
  phone,
  contactName,
  bedOption,
  noOfBeds,
}) => {
  return <div>{name}</div>;
};

export default Hostel;
